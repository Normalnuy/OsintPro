<Window x:Class="OsintPro.UI.Views.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оновлення Justin OSINT" Height="580" Width="750"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="NeonScrollThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder" Background="#00C6FF" CornerRadius="3" Margin="2" Opacity="0.5"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Opacity" Value="1"/>
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#0072FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="#0CFFFFFF" CornerRadius="4">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource NeonScrollThumbStyle}"/>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Border Background="#121215" CornerRadius="20" BorderBrush="#5500C6FF" BorderThickness="1.5">
            <Border.Effect>
                <DropShadowEffect Color="#00C6FF" BlurRadius="40" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
        </Border>

        <Grid>
            <Grid.Clip>
                <RectangleGeometry Rect="0,0,700,530" RadiusX="20" RadiusY="20"/>
            </Grid.Clip>

            <Ellipse Width="400" Height="400" Fill="#0072FF" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-150,-150,0,0" Opacity="0.25">
                <Ellipse.Effect>
                    <BlurEffect Radius="90"/>
                </Ellipse.Effect>
            </Ellipse>

            <Ellipse Width="500" Height="500" Fill="#00C6FF" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-200,-200" Opacity="0.2">
                <Ellipse.Effect>
                    <BlurEffect Radius="120"/>
                </Ellipse.Effect>
            </Ellipse>

            <Path Data="M 0 0 L 100 100 M 100 0 L 0 100 M 200 0 L 100 100" Stroke="#11FFFFFF" StrokeThickness="2" Stretch="UniformToFill" Margin="20" Opacity="0.5"/>
        </Grid>

        <Grid Margin="35">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Viewbox Width="65" Height="65" Margin="0,0,20,0" VerticalAlignment="Center">
                    <Viewbox.Effect>
                        <DropShadowEffect Color="#00C6FF" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
                    </Viewbox.Effect>
                    <Canvas Width="100" Height="100">
                        <Polygon Points="10,40 45,55 35,15" Fill="#0072FF" Opacity="0.8"/>
                        <Polygon Points="10,40 45,55 30,65" Fill="#005bb5" Opacity="0.6"/>
                        <Polygon Points="90,30 55,55 65,10" Fill="#00C6FF" Opacity="0.9"/>
                        <Polygon Points="90,30 55,55 75,55" Fill="#0099cc" Opacity="0.7"/>
                        <Polygon Points="45,55 55,55 50,90" Fill="#00C6FF" Opacity="0.9"/>
                        <Polygon Points="45,55 55,55 65,35 40,30" Fill="#0072FF" Opacity="0.9"/>
                        <Polygon Points="40,30 25,25 45,40" Fill="#00C6FF"/>
                    </Canvas>
                </Viewbox>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="С И С Т Е М Н Е   О Н О В Л Е Н Н Я" FontSize="12" FontWeight="Bold" Foreground="#0072FF" Margin="0,0,0,2"/>
                    <TextBlock x:Name="VersionTitle" Text="JUSTIN OSINT v.X.X.X" FontSize="26" FontWeight="Black" Margin="0,0,0,4">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FFFFFF" Offset="0.0"/>
                                <GradientStop Color="#00C6FF" Offset="1.0"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>
                    <TextBlock Text="Глобальне покращення стабільності та нові модулі" FontSize="14" Foreground="#A0A0A0"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Height="1" Background="#3300C6FF" Margin="0,0,0,20"/>

            <Border Grid.Row="2" Background="#0CFFFFFF" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="12" Padding="20">

                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,0,-10,0" FocusVisualStyle="{x:Null}" Focusable="False">
                    <RichTextBox x:Name="ChangelogDocument"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 IsReadOnly="True"
                                 IsTabStop="False"
                                 Focusable="False"
                                 Margin="0,0,10,0"
                                 FontFamily="Segoe UI"
                                 Foreground="#E5E5E5"/>
                </ScrollViewer>
            </Border>

            <Button Grid.Row="3" Margin="0,25,0,0" Height="50" Click="CloseButton_Click" Cursor="Hand" FocusVisualStyle="{x:Null}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" CornerRadius="10" Background="#005bb5" BorderBrush="#00C6FF" BorderThickness="1" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Grid>
                                <Border x:Name="GlowBorder" CornerRadius="10" Background="#00C6FF" Opacity="0">
                                    <Border.Effect>
                                        <BlurEffect Radius="15"/>
                                    </Border.Effect>
                                </Border>
                                <TextBlock Text="І Н І Ц І А Л І З У В А Т И   С И С Т Е М У" Foreground="White" FontWeight="Bold" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="GlowBorder" Storyboard.TargetProperty="Opacity" To="0.7" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#0072FF" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="GlowBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="BtnBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#005bb5" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>