<Window x:Class="OsintPro.UI.Views.AppDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Justin OSINT" Height="Auto" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize">

    <Border Background="#121215" CornerRadius="16" BorderBrush="#5500C6FF" BorderThickness="1.5" Margin="20" Padding="28">
        <Border.Effect>
            <DropShadowEffect Color="#00C6FF" BlurRadius="30" ShadowDepth="0" Opacity="0.35"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="DialogIcon" Grid.Row="0" FontSize="32" Margin="0,0,0,12" HorizontalAlignment="Left"/>
            <TextBlock x:Name="DialogTitle" Grid.Row="1" FontSize="18" FontWeight="Bold" Foreground="#00C6FF" Margin="0,0,0,10" TextWrapping="Wrap"/>
            <TextBlock x:Name="DialogMessage" Grid.Row="2" FontSize="14" Foreground="#D0D0D0" TextWrapping="Wrap" LineHeight="22" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SecondaryButton" Content="Скасувати" Click="Secondary_Click" Visibility="Collapsed"
                        MinWidth="110" Height="40" Margin="0,0,10,0" Background="#2A2A2A" Foreground="#AAA"
                        BorderBrush="#444" BorderThickness="1" Cursor="Hand" FocusVisualStyle="{x:Null}"/>
                <Button x:Name="PrimaryButton" Content="OK" Click="Primary_Click"
                        MinWidth="120" Height="40" Background="#0072FF" Foreground="White" FontWeight="Bold"
                        BorderBrush="#00C6FF" BorderThickness="1" Cursor="Hand" FocusVisualStyle="{x:Null}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>